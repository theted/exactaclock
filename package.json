{
  "name": "exactaclock",
  "version": "1.0.0",
  "description": "GPS-based time correction application that displays corrected GMT time based on your exact coordinates",
  "main": "index.html",
  "scripts": {
    "start": "npx http-server . -p 8080 -o",
    "serve": "npx http-server . -p 8080",
    "test": "echo 'Open http://localhost:8080/test.html in your browser after running npm start'",
    "dev": "npx http-server . -p 8080 -o",
    "lint": "eslint src/**/*.js",
    "lint:fix": "eslint src/**/*.js --fix",
    "format": "prettier --write src/**/*.js",
    "format:check": "prettier --check src/**/*.js",
    "help": "echo 'Available commands: npm start (run app), npm test (info about tests), npm run dev (development server), npm run lint (check code), npm run format (format code)'"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  },
  "keywords": ["timezone", "gps", "clock", "time", "location", "coordinates", "javascript", "html5"],
  "author": "",
  "license": "MIT",
  "homepage": "index.html",
  "repository": {
    "type": "git",
    "url": "."
  }
}